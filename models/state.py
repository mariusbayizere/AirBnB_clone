#!/usr/bin/python3
"""Define class state"""
from models.base_model import BaseModel


class State(BaseModel):
    """represent the class state.

    property:
        name (str): name of the state.
    """

    name = ""
